/** @type {import('next').NextConfig} */
const nextConfig = {
    // 必要な設定をここに追加します
    experimental: {
      // Turbopackを有効にする場合
      // turboMode: true,
    },
  };
  
  module.exports = nextConfig;
  